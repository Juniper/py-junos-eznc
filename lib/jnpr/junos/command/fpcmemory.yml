---
FPCMemory:
    command: show memory
    target: fpc1
    grep: Kernel
    key: ID
    view: FPCMemoryView

FPCMemoryView:
    column:
        id: ID
        base: Base
        total: Total(b)
        free: Free(b)
        used: Used(b)
        perc: "%"threads.yml
        name: Name
