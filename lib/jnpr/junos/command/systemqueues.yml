---
SystemQueuesOPTable:
  command: show system queues
  key: output interface
  view: SystemQueuesOPView

SystemQueuesOPView:
  columns:
    w: output interface
    bytes: bytes
    max_bytes: max
    packets: packets
    max_packets: max
    drops: drops

SystemQueuesINTable:
  command: show system queues
  key: input protocol
  view: SystemQueuesINView

SystemQueuesINView:
  columns:
    interface: input protocol
    bytes: bytes
    max_bytes: max
    packets: packets
    max_packets: max
    drops: drops

