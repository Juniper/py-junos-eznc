<security>
    <nat>
        <source>
            <pool>
                <name>{{ pool_name }}</name>
            </pool>
            <rule-set>
                <name>{{ ruleset_name }}</name>
{%- if not NAMES_ONLY %}
                <from/>
                <to/>
{%- endif %}                
                <rule>
                    <name>{{ rule_name }}</name>
                </rule>
            </rule-set>
        </source>
    </nat>
</security>