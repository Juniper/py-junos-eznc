---
FPCThread:
  command: show threads
  target: None
  key: Name
  view: FPCThreadView

FPCThreadView:
  columns:
    pid_pr: PID PR
    state: State
    name: Name
    stack: Stack Use
    time: Time (Last/Max/Total)
    cpu: cpu
  filters:
    - cpu
    - state
